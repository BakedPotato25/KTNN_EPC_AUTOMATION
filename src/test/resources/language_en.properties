# UI label / message keys used by DataModel.getTitle() and WebUI.getLanguageValue().
# Add one key per label that needs to render differently in report messages.
# Example:
# UserName=Username
# Password=Password
